{
  "name": "@padrone/example",
  "version": "0.0.1",
  "private": true,
  "module": "src/index.ts",
  "scripts": {
    "start": "bun run src/index.ts",
    "ai": "bun run src/ai.ts",
    "dev": "bun run --watch src/index.ts",
    "build": "bun build src/index.ts --target bun",
    "compile": "bun run build --compile"
  },
  "dependencies": {
    "ai": "^6.0.0-beta.149",
    "padrone": "workspace:*",
    "zod": "^4.1.13"
  }
}
