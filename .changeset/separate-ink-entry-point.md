---
'padrone': major
---

The Ink integration (`padroneInk`, `isReactElement`, `InkOptions`) now lives at a separate entry point and must be imported from `'padrone/ink'` instead of `'padrone'`. This keeps React/Ink out of the main bundle.
